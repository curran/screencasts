// Get the DOM element that will contain the posts.
var postsDiv = document.getElementById("posts");

// Set the HTML content inside the div.
postsDiv.innerHTML = '<div style="color: red;">Hello innerHTML!</div>';
